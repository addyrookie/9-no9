<h2> All the news u need </h2>
<div id  = "recent_episodes" >
  <h3>Recent Railscasts </h3>
 <ul> 
  <% for entry in FeedEntry.all(:limit => 20 , :order => "published_at desc") %>
     <li> <%= link_to h(entry.name) , entry.url %>
          <%= entry.summary %>
     </li>
    <% end %>      
  </ul>
</div>
